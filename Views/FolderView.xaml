<reactiveUi:ReactiveUserControl x:TypeArguments="viewModels:FolderViewModel" x:Class="RevitServerViewer.Views.FolderView"
                                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                                xmlns:local="clr-namespace:RevitServerViewer"
                                xmlns:reactiveUi="http://reactiveui.net"
                                xmlns:viewModels="clr-namespace:RevitServerViewer.ViewModels"
                                mc:Ignorable="d"
                                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                d:DesignHeight="50" d:DesignWidth="300">
        <Grid HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <CheckBox Name="SelectedBox"></CheckBox>
            <TextBlock HorizontalAlignment="Stretch" VerticalAlignment="Center" Grid.Column="1" Name="NameBox"></TextBlock>
        </Grid>
</reactiveUi:ReactiveUserControl>