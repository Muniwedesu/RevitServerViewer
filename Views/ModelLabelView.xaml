<reactiveUi:ReactiveUserControl x:TypeArguments="viewModels:ModelLabelViewModel"
                                x:Class="RevitServerViewer.Views.ModelLabelView"
                                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                                xmlns:local="clr-namespace:RevitServerViewer"
                                xmlns:reactiveUi="http://reactiveui.net"
                                xmlns:viewModels="clr-namespace:RevitServerViewer.ViewModels"
                                mc:Ignorable="d"
                                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="24,0,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <CheckBox Name="SelectedBox"></CheckBox>
        <TextBlock VerticalAlignment="Center" Grid.Column="1" Name="NameBox"></TextBlock>
    </Grid>
</reactiveUi:ReactiveUserControl>